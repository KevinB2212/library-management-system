# 📖 Library Management System

> Django web app for managing a library catalogue

## About

A library management system built with Django. Allows users to browse, search, and manage a collection of books through a web interface with an admin panel.

## Project Structure

```
├── Library/
│   ├── Library/       # Django project settings
│   ├── templates/     # HTML templates
│   ├── db.sqlite3     # SQLite database
│   └── manage.py      # Django management script
```

## Getting Started

```bash
cd Library
pip install django
python manage.py migrate
python manage.py runserver
```

Then open `http://localhost:8000` in your browser.

## Tech Stack

- **Backend:** Python, Django
- **Frontend:** HTML
- **Database:** SQLite

## Author

**Kevin** — Dublin City University
